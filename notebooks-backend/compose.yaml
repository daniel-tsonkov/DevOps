name: notebooks-backend

services:
  notebooks:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 3000:80
    environment:
      - PORT=80
    develop:
      watch:
        - path: ./src
          action: sync
          target: /app/src